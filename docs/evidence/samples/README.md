# API samples (illustrative)

Replace field values with your own captures for the thesis. These files use **placeholder** hashes and structure only.

| File | Purpose |
|------|---------|
| `upload-request-response.example.json` | `POST /api/upload` body + typical success fields |
| `query-request-response.example.json` | `POST /api/query` body + typical success fields |
| `audit-events.example.jsonl` | Two lines showing `RecordCreated` / `ProposalExecuted` shape from `eventListener` |
