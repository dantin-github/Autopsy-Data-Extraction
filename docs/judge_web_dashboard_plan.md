# Judge Web dashboard plan (working copy)

Mirrors dissertation milestones for the Streamlit **Judge Dashboard** and related gateway UX. Original Cursor plan id: `1e12b7be` (not versioned in-repo).

## Phase 5 · CaseRegistry operator alignment

| Id | Item | Status |
|----|------|--------|
| **S5.1** | **Deploy CaseRegistry** (`api-gateway`: `npm run deploy-contract`) **visible from Judge Web**: `GET /health` exposes `gateway` capabilities; sidebar **CaseRegistry (S5.1)** shows fields after **Ping Gateway**. | Done |

## References

- `api-gateway/README.md` — Phase 5 · Deploy CaseRegistry (S5.1)
- `docs/project-progress.md` — implementation log
