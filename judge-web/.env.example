# Streamlit listens on this port (pass via: streamlit run app.py --server.port 8501)
# PORT=8501

# API Gateway base URL (used from Phase S1.2 onward)
API_GATEWAY_URL=http://localhost:3000

# Optional: trace|debug|info|warn|error
LOG_LEVEL=info
